export enum OrderStatus {
    pending = 'pending',
    verified = 'verified',
    denied = 'denied',
    failed = 'failed'
}

export enum Role {
    employee = 'employee',
    manager = 'manager',
    admin = 'admin'
}